# Optional. Used only if you later connect an LLM provider from your own environment.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ReferralReady local settings
REFERRALREADY_DATA_DIR=./data
REFERRALREADY_SYNTHETIC_ONLY=true
